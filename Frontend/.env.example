# Leave empty in development — Vite proxies /api to localhost:5000
# In production, set your API origin (example: https://api.example.com)
# Do NOT include /api at the end.
VITE_API_URL=

# Legacy name also supported by the client:
# VITE_API_BASE_URL=
